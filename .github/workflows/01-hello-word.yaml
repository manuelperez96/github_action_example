name: 01-hello-world

on:
    push:
        branches: [main]

jobs:
    hello:
        runs-on: ubuntu-24.04
        steps:
            - name: Saludar
              run: echo "Hola desde github action"
            - name: Fecha
              run: date
            - name: Ver archivos disponibles
              run: ls -la

    repo-files:
        runs-on: ubuntu-24.04
        steps:
            - name: Descargar archivos del repo
              uses: actions/checkout@v6
            - name: Ver archivos disponibles
              run: ls -la
