name: 01-hello-world

on: 
    push:
        branches: [main]

jobs:
    hello:
        runs-on: ubuntu-24.04
        steps:
            - name: Saludar
              run: echo "Hola desde github action"
            - name: Fecha
              run: date
            - name: Ver archivos disponibles
              run: ls -la
